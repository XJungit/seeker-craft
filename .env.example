# Craft-Agent 环境变量示例（复制为 .env 并填入真实值）
# .env 已被 .gitignore 忽略，不会进版本库；本文件仅作模板提交。

# 决策 / 视觉模型密钥（agent.toml 的 [llm]/[vlm] 后端按 api_key_env 读取）
MINICPM_API_KEY=
LONGCAT_API_KEY=
STEPFUN_API_KEY=

# 专用压缩模型密钥（agent.toml 的 [compaction] 后端读取）
# 用免费、512K 上下文的 agnes-2.0-flash 做历史压缩，隔离主决策模型。
AGNES_API_KEY=

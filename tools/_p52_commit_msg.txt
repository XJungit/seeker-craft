P52: prompt/profile 对齐 9.2 铁律（furnace/pickaxe 禁止 auto_craft）

修复 LLM 不规划多步合成的根因之一：prompt/profile/few-shot 与 9.2 铁律
承诺撕裂。原 prompt 反复示范 auto_craft(furnace/wooden_pickaxe/...)，
但 9.2 已禁止这些工具方块/工具走 auto_craft（会返回 Err）。LLM 抄了
few-shot 调 auto_craft 被拒后没有可参考的「先 craft_3x3 → place → open」
正确链路示范，导致原地重试或放弃。

修改：
1. crates/craft-agent/src/agent/prompt.rs
   - 铁矿→熔炼铁锭示例：删除 auto_craft(furnace)，改为完整链路
     gather cobblestone → craft_3x3(furnace) → place → open →
     gather coal → smelt(iron_ingot)
   - 食物烤肉示例：同样删除 auto_craft(furnace)，改为完整链路
2. profiles/_default.json
   - system_prompt：删除「auto_craft 一键搞定复杂合成」的兜底承诺，
     明确列出禁止 auto_craft 的工具方块/工具清单
   - mc_knowledge 合成链路段：重写为正确的 craft/craft_3x3/smelt 链路
   - mc_knowledge 照明段：auto_craft(torch) → craft(torch)
   - mc_knowledge 目标拆解段：删除「优先 auto_craft」误导
   - mc_knowledge 首日生存段：重写为完整 13 步链路（gather→craft→
     place→open→craft_3x3→gather→craft_3x3→...→smelt）
   - mc_knowledge 关键工具时机段：auto_craft(torch) → craft(torch)

回归测试：cargo test regression_ 全部通过（system prompt 字节稳定性
+ 无动态标记 + 任务目录加载）。LLM 实机验证留待下一轮全自动化测试。

P50+P51: 配方书合成/锻造/切石机产物收集验证对齐

P50 (2026-07-27): do_craft_3x3_recipe 和 do_craft_smithing 加上
count_item_in_player_slots 前后对比 + left_click 兜底，与 P49 的
smelt 验证模式一致。原 code 直接 += 1 不验证，背包满时 made 虚增。

P51 (2026-07-27): do_craft_stonecutter 三处修复：
1. 输入槽位错误：原 code 把 input 放入 slot 1（结果槽），
   服务端永远算不出结果。改为 slot 0（input slot）。
2. 加入产物收集验证（shift_click + left_click 兜底 + 背包满明确报错）。
3. 循环外只 fetch 一次 inv 改为每轮重新 fetch，避免 stale state。

# dsh-bridge bundle patch: activates the plugin row in any profile
# that installs this package (via `dsh plugin --profile <name> add ...`).
# The user's own cordis.patch.yml may still override or disable this row by id.
- insert:
    - id: dsh-bridge
      name: dsh-bridge

[package]
name = "craft-agent-autopilot"
version.workspace = true
edition.workspace = true
description = "Craft-Agent 全自动化自进化主循环"

[[bin]]
name = "craft-agent-autopilot"
path = "src/main.rs"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde_json = { workspace = true }
serde = { workspace = true }

# Rust 构建产物
/target

# 密钥 / 本地配置（切勿提交）
.env
config/.env
*.env

# 本地 LLM 后端配置（含真实 api_key）；开源仓库只提交 agent.example.toml
/data/config/agent.toml

# opencode 工作笔记（含内部日志与历史密钥记录，仅本机）
.opencode/

# 运行期数据（会话 JSONL / 事件 / 报告）
/sessions/
/data/memory/


# 临时文件
*.tmp
*.png
*.pt
_tmp_*
smoke_*.txt

# 但 docs/images/ 下的 README 截图要提交（上面 *.png 的例外）
!docs/images/
!docs/images/*.png

# 大模型文件已移除（mod 桥接后不再需 ONNX 检测模型）

# Python 缓存
__pycache__/
*.pyc

# 本地 agent 记忆（机器相关，非源码）
.workbuddy/

# 本地 cargo 配置（含机器相关绝对路径，勿提交；仓库用 https git 依赖）
.cargo/config.toml

# 参考项目源码（学习用，非项目主源码）
references/

# 参考项目克隆（学习用，非项目源码）
.mindcraft-clone/
.numen-clone/
.touhou-clone/

# ===== Fabric mod 构建产物（craft-agent-bridge）====
/mods/craft-agent-bridge/build/
/mods/craft-agent-bridge/.gradle/
/mods/craft-agent-bridge/tools/proxy_cache/
/mods/craft-agent-bridge/local-maven/
/mods/craft-agent-bridge/build_log*.txt
/mods/craft-agent-bridge/*.log
/mods/craft-agent-bridge/javatest*

compile-log.txt

# 运行产物（viewer session JSONL）
/sessions/
/scripts/logs/
/tools/sessions/

# 快捷方式（机器相关，非源码）
*.lnk

# 工作流状态
.workflow_state.json

# DSH 本地插件依赖链接（指向本机 DSH/npx 安装，机器相关，勿提交）
/tools/dsh-bridge/node_modules/

# Mimosa 安全插件运行时数据（hook 源码快照/状态，私有，勿提交）
.mimosa/

# viewer 运行日志
/logs/

.rust-analyzer.json

# Agent 工作信息（本机 agent 使用，与项目源码无关，勿提交）
AGENTS.md
/.agents/
/.zcode/
